# Changelog

Newest first. One line per version: `{version} {ISO 8601 timestamp with local offset} {summary}`

0.0.40 2026-09-23T22:35:34-07:00 `move` asks whether the user's words express a status, not which status is closest. 0.0.39 asked for "a word only one of them could mean", and a Codex run on a second backlog ticket showed the model reading that as a choice among the seven: `move 196 in reveal` moved #196 to `in-review`. The row now says a word that only looks like a status name expresses none, nor does one the model had to pick a status for, so it changes nothing and asks. The 0.0.39 rule against reaching a terminal status from any word but its name is removed, since a word like `cancelled` does mean `cancel`. The runbook's A17b, B9b, and C6 use one ticket per step, check the session for write commands, and add a terminal look-alike that must not close and a terminal meaning that may close or ask.
0.0.39 2026-09-23T21:43:34-07:00 `move` asks instead of guessing a status. A manual Codex run of 0.0.38 read `in reveal`, `in revolt`, and `in rewind` as `in-review` on all three backends, and `in revolt` moved CLE-105 from In Progress to In Review. The `move` precondition now says the user named one of the seven or a word only one of them could mean, that a status is what everyone else acts on and a terminal one cannot be undone, so a guess becomes a question listing the seven, and that a terminal status never comes from a word that is not its name. The runbook's A17b, B9b, and C6 now run the unclear word on a second backlog ticket, so a guess shows in state, and add a near-terminal word that must not close the ticket.
0.0.38 2026-09-22T21:18:07-07:00 `create` reads everything it is given as the ticket: a word that names a status or another verb is ticket text, not a request. 0.0.37 said `create` runs no other verb, and a manual Kiro run obeyed it and then ran `move` as a second step of its own, making #186 `Alpha manual5 in review` at `in-review`. Section 5 now says what a ticket carries (title, body sections, `## Blocked by` edges, a milestone) and that `move` and `assign` set status and assignee, following agent-toolkit's split where the tracker takes a ticket and a separate skill turns a request into one.
0.0.37 2026-09-22T20:16:52-07:00 `create` sets no status and runs no other verb. 0.0.36 ended the `create` row with "`move` gives it any other status", and a manual run showed both Kiro CLI and Claude Code reading that as an instruction to run `move` after `create`: `create Alpha manual4 in review` made `Alpha manual4` at `in-review` on both. The row now states that `create` sets no status and runs no other verb.
0.0.36 2026-09-22T19:43:43-07:00 `create` takes no status. It always writes `backlog`, and `move` sets any other status. A manual Kiro run showed why: interactive Kiro strips quotes, so `create "Alpha manual3 in review"` reached the model as `create Alpha manual3 in review`, and no syntax could separate a title from a trailing status. Removing the argument removes the ambiguity. `SKILL.md`, the three backend files, and the tracker README drop the status-last sequence that existed only to apply a requested status after the edges. The runbook's A17c now tests that `create` writes `backlog` and drops the contrived `in reviewww` milestone; B4, C23, and C24 no longer create with a status; and A17b, B9b, and C6 accept either a move to the status an obvious typo means or a question, never another status.
0.0.35 2026-09-22T16:17:41-07:00 Cut the remaining input-guard text, with no change in behavior. Removed: the `12`, `012`, `#12` id-form rule from `SKILL.md` and `local.md`; the unknown-milestone stop repeated in `github.md`, `linear.md`, and `local.md`, now one sentence in `SKILL.md`; the local slug algorithm and its Unicode example, now "a short, lowercase, hyphenated form of the title"; "compared case-insensitively" on `from <holder>`; "say which of the three it was" for a missing config; and the line repeating that `ready` clears the assignee. Kept: measured backend facts and the multi-agent rules (the assign tie-break, named holders, status-last create). The runbook's B2 and B3 check only that a filename starts with its id.
0.0.34 2026-09-22T16:10:46-07:00 Stop teaching the model to read. 0.0.33 still told it how to handle case, hyphens, typos, unclear words, terminal typos, milestone precedence, and ambiguous `create` text. That is spell-check policy, and a tracker does not need it: the model reads the seven statuses from the table, knows from section 2 that terminal statuses are final, and can ask when unsure. The paragraph, the read-step sentence about stating the status, and the tracker README paragraph are gone; `SKILL.md` is 211 lines. The runbook drops the terminal-typo steps from A17b and C6, which only that rule required.
0.0.33 2026-09-22T16:05:44-07:00 Read a status the way a person would, and drop the parse grammar. 0.0.29 to 0.0.32 made the model write a fixed parse line, gave `create` a quoted versus unquoted grammar with seven line shapes, and refused every near miss. That caused its own failures: a model copied a parse example word for word and dropped `done`, and interactive Kiro strips quotes, so the quoted form could never be sent there. `SKILL.md` now says: case does not matter, a space reads as a hyphen, an obvious typo means the status it misspells (`redy` is `ready`), and the reply names the status used. A word with no clear meaning is refused with the seven listed. A typo never closes a ticket: when the obvious reading is terminal, ask, since no verb reopens one. `list` takes an existing milestone name first. `create` asks only when it cannot tell whether the last words are status or title. The parse step, its shapes, and the quote rule are gone, and `SKILL.md` is 223 lines. The tracker README says the same. The runbook's A17b, A17c, B9b, and C6 now check outcomes: the status written, no write on an unclear or terminal typo, and a reply that matches what was created. They no longer check wording or the order of calls.
0.0.32 2026-09-22T15:35:04-07:00 Settle when the parse line comes. `SKILL.md` section 1 said to read the config and the backend file before running anything, and the section 3 parse step said to write its line before any tool call. Codex on `gpt-6-luna` wrote the line first; Kiro CLI, headless and interactive, made the two reads first, and wrote nothing before the line either way. The parse step now comes after section 1's two reads and before any other tool call. The runbook's A17c step 3 and C6 transcript checks allow the skill load and those two reads before the line. A17b and B9b now fail a refusal that suggests a status, as C6 already did; Kiro's refusals asked "Did you mean `in-review`?", which `SKILL.md` forbids and only C6 checked. The two Kiro run log entries carry a note re-scoring them under these checks.
0.0.31 2026-09-22T13:30:55-07:00 Give `create` a parse line for every form. The 0.0.30 run on Codex refused every bad `move` status with no tool call, 7 of 7, but A17c failed two ways. Four of five unquoted `create <words> in review` calls wrote a status-form line such as `create: "in progress" is in-progress` and created the issue instead of asking. `create "Delta" done` wrote `create: no status, backlog`, a parse example copied word for word, and created the issue with `done` dropped, because no example covered a `create` given a status. `SKILL.md` now makes a `create` line say first whether the ticket is quoted, lists a shape for each form (quoted with an open status, quoted with a terminal status refusing, quoted with none, not quoted asking, not quoted with none), and says a `not quoted` line never names a status. Section 2, the `create` precondition, and the verb table say a status follows only a quoted ticket. The tracker README says the same. The runbook logs the run and checks step 4's parse line.
0.0.30 2026-09-22T13:13:19-07:00 Stop teaching to the test. 0.0.29 used `in reviewww`, the exact string runbook cases C6, A17b, and B9b send, as the refusal example in `SKILL.md` and the tracker README, and `create Fix in review`, the exact form A17c and B9b send, as the ask example. A pass could then mean the model matched an example rather than applied the rule. The examples are now `move 12 redy`, `create Fix login ready`, and `"In Review" is in-review`, which no case sends. The runbook adds a rule that a test input never appears in the skill files, checked with `grep` before a run, and rotates the input across repeats of C6 and A17c step 3.
0.0.29 2026-09-22T13:08:43-07:00 Make the status check a visible step. A targeted 0.0.28 run on Codex still found `move <id> in reviewww` written as In Review on one of three Linear repeats, and an unquoted `create … in review` created an in-review issue on one of three GitHub repeats. In each failure the model went straight to the write without stating any check; rule placement in 0.0.27 and 0.0.28 did not change that. `SKILL.md` section 3 now starts `create` and `move` with a parse step: before any tool call, one line saying what the status argument is, and a line ending in refusing or asking stops the verb with no tool call. Section 2 gains the example `move 12 in reviewww` refuses without suggesting `in-review`. The tracker README describes the line. The runbook logs the run, requires at least three repeats of C6 and A17c step 3, and checks that the parse line comes before any tool call.
0.0.28 2026-09-22T12:58:22-07:00 Finish moving the status-argument rules into the section 3 preconditions. A targeted 0.0.27 run on Codex found the `move` check holding on all three repeats, but one refusal suggested `in-review` instead of listing the seven statuses, and `create Charlie t1250 in review` created an in-review issue without asking. Both rules lived only in section 2. The `create` precondition now says unquoted ticket text ending in a status name gives no status, so ask and write nothing; the `move` precondition says refuse, list all seven, and suggest none. The runbook logs the run, runs A17c step 3 three times, and fails a C6 refusal that suggests a status.
0.0.27 2026-09-22T12:47:29-07:00 Put the status check where the Read step looks. A targeted 0.0.26 run on Codex found `move <id> in reviewww` on Linear written as In Review: the child followed `linear.md`'s `move` row, and the refusal lived only in `SKILL.md` section 2. The section 3 preconditions now say that a `move` target is one of the seven statuses, never the nearest match, and that a `create` status is one of the four open ones. The runbook logs the run, re-scored by hand, and C6 now runs three times.
0.0.26 2026-09-22T11:48:12-07:00 Fix four tracker defects from an independent review of the trim. Linear `move <id> duplicate <original>` said the transition clears the issue's relations; measured on team `c-leg`, it moves `blockedBy`, `blocks`, and `relatedTo` onto the original and silently drops one that would point the original at itself or at a ticket it already relates to. `linear.md` now names the relations that will move, runs the `link` cycle walk on each moved edge and refuses a cycle, verifies the original afterwards, and the generic `move` row routes `duplicate` to that section. Unfiltered GitHub `list` returned an inconsistent issue as a normal ticket as well as under `inconsistent`, a defect older than the trim; it now selects `status != "inconsistent"`. `SKILL.md` names local `create` as the one-write exception to the staged create sequence, and a failed create reports the status the ticket now has. The strict status rule now covers only a status position: a non-status `list` argument is a milestone, and an unquoted `create` whose text ends in a status asks before writing. Rejected: making `move backlog` leave no label. The runbook corrects the Linear duplicate evidence, rewrites C9, and extends A12, B4, B9b, and a new A17c.
0.0.25 2026-09-22T08:47:21-07:00 Fix two tracker defects found by the post-trim run. GitHub `create` at `backlog` added a `backlog` label because `github.md` said only "add the requested status label"; it now says a `backlog` ticket stays unlabelled, and the verification expects no status label. A status argument that is not one of the seven statuses, such as `in reviewww`, was guessed as the nearest status and written; `SKILL.md` now refuses it, lists the seven, and writes nothing, matching case-insensitively with a space read as a hyphen. Runbook case C6 now runs through the skill, and new cases A17b and B9b cover the same refusal on GitHub and local.
0.0.24 2026-09-22T00:43:12-07:00 Trim `SKILL.md` from 2056 to about 1840 words by removing reasons next to rules that stand alone and a paragraph that gave no instruction; every rule stays. Restore detail 0.0.23 cut that a verb needs: Linear's `Could not find state` string as the missing-status signal, what Linear rewrites in a description, not guessing whether an archived issue was completed or deleted, and that the tie-break never fires on Linear; local's no-escape rule inside single quotes, the slug example, and that the slug carries no meaning.

0.0.23 2026-09-22T00:35:28-07:00 Cut the three tracker backend files to rules, commands, and checks, since one of them loads on every tracker call: `github.md` 2302 to 1299 words, `linear.md` 1728 to 820, `local.md` 1309 to 632. Explanations and measurements behind the rules move to the runbook's evidence section. No rule, command, or check changed. Runbook references to removed wording in B12b, C10, and C20 now point at the rule or the evidence.

0.0.22 2026-09-21T23:53:52-07:00 Fix local `assign <id> me from <holder>` writing the literal `me`: `local.md` now resolves `me` to `git config user.name` in every form (B15, caught by the targeted 0.0.21 run). `linear.md` no longer claims every rejected relation arrives as a warning: C24 showed an unknown blocker id fails the whole call, while a self-link still succeeds with a warning, which is what the Backlog-first create guards against.

0.0.21 2026-09-21T16:48:18-07:00 Clean the tracker skill for people who never saw its validation. Remove test evidence, dates, fixture ids, and history from SKILL.md and the three backend files, and move what was not already recorded into a new "Evidence behind the skill files" section of validation/tracker.md. Fix contradictions: SKILL.md no longer says a closed ticket is done, `create` states that a backend may write edges and status together, and README no longer says a bad Linear status name succeeds or that a race makes the skill stop. Settle ambiguities: `assign <id> me from <holder>` is an explicit form and leaves the status alone, GitHub's `assign <id> none` has a command, and the GitHub jq prelude now holds `open_blockers` and resolves closed issues too, so `show` uses it. Linear `create` now scopes to the project and sets the milestone, writes the edges at Backlog, and applies the requested status only after a read confirms every edge, because Linear reports a rejected relation as a warning on a successful call. `create` skips the cycle walk on every backend, since nothing can be blocked by a ticket that does not exist yet. Linear `list` with no status makes one call per open team status, unmapped ones included, so an issue in a custom status is still listed. Local `create` is one write. Runbook cases C23, C24, and C25 cover the Linear changes, and A29c and C14 check that `assign <id> me from <holder>` leaves the status alone. Merge repeated GitHub rules on the search index and truncation into one place each.

0.0.20 2026-09-21T16:13:13-07:00 Make `link` refuse a self-link and any edge that would close a cycle, and make an empty `next` say why. Both come from asking whether the tracker can serve an orchestrator with nobody watching, where "nothing to do" and "everything is stuck" must not look alike. Probed live on 2026-09-21: GitHub refuses a self-link and a two-issue cycle with HTTP 422 but accepted the third edge of #35 → #37 → #36 → #35; Linear accepted CLE-5 → CLE-6 → CLE-4 → CLE-5, refused a self-link only in the `warnings` of a successful response, and, given the reverse of an existing edge, silently replaced it, so CLE-5 lost its blocker with no error. `link` now walks the blocker's open blockers first and refuses with the path; its verification also reads the blocker, to catch Linear's flip. An empty frontier names every held `ready` ticket with its assignee or its open blockers and their status and holder, and any cycle as a path. `github.md`'s `next` query gains `held` and `open` in the same read, tested against `wilsonkichoi/tracker-gh`. Runbook cases A31, A32, B19, B20, C21 and C22 cover both behaviours on all three backends.

0.0.19 2026-09-21T15:02:31-07:00 Stop `tracker` when `docs/dev-agents/config.md` cannot name a backend (F25). With no config, no `issue_tracker` field, or a value outside `github`, `linear`, `local`, `other`, section 1 now writes nothing, reads no backend file, and tells the user to run `setup` with their harness's prefix; another tool's config or ticket format is never a substitute. A void Codex run had loaded the skill in a directory with no config and written tickets as `.dev/tasks/T-NNN-*.md` in another plugin's format, then reported success. The tracker README says the same. The runbook gains S4 (no config) and S5 (`issue_tracker: jira`), both checked on disk before any setup, and warns that a harness can list a same-named skill from a plugin, which voids a run that fires it.

0.0.18 2026-09-19T23:34:05-07:00 Teach `linear.md` about archived issues, and point C18 at the UI action Linear actually has. The five outstanding [MANUAL] Linear cases ran against a throwaway team, `c-leg`, created from Linear's default six-status template: C1, C2, C3 and C5 passed, which closes the whole status-drift contract on Linear for the first time, and C18 failed. F23, the skill defect: `list_issues` defaults `includeArchived` to `false` and `linear.md` never mentioned archiving at all, so `list done` on a team whose only closed ticket was archived returned `{"issues":[],"hasNextPage":false}`, an empty page that reports itself as complete and reads exactly like a team that has closed nothing. The same call with `includeArchived: true` returned it. The file now splits the read: `done`, `cancel` and `duplicate` pass `includeArchived: true`, ask for `archivedAt`, and mark the archived results, because a closed ticket is a record of work and dropping it in silence is the worse answer, while the four open statuses and `next` keep the default, since a frontier offering a ticket somebody deleted is worse than one that comes back short. `show` already worked, `get_issue` returning an archived issue in full, and now says that it is archived and when. Two different things set `archivedAt`, automatic archiving after inactivity and a user deleting an issue into Recently deleted, and this server distinguishes them nowhere: no `trashed` flag on any tool and no auto-archive period on `get_team`, so the report says archived and does not guess. F21, the runbook fix: C18 told the runner to archive an issue in the UI, and Linear has no manual Archive action, so the case was unperformable as written and scored SKIP on its first attempt. Delete is the reachable action, it is a soft delete into Recently deleted, and it sets the same field, so C18 now names it and says it is restorable. F22 stands unfixed and is recorded as a limit rather than a defect: equal archived and unarchived counts on a young project prove only that nothing has archived yet, and the auto-archive policy cannot be read through MCP at all.

0.0.17 2026-09-19T22:47:10-07:00 Record in `linear.md` that a completed milestone stays visible to the resolver, and log the targeted re-run that closed F19 and F20. The re-run of C10 and C20 against c954b3f came back 2 of 2 on Codex, and the independent reads hold: `list_milestones` then one complete `list_issues` page with `projectMilestone` in `fields`, filtered locally, returned only the member issue and never passed the rejected `milestone` argument; the unknown name stopped and named the two milestones that exist; and DEV-23 came back with its five headings in order, `Café, 東京, and naïve résumé` unchanged, the fenced `λ = "snow ☃"` block identical including its indented `<tag>` and its two backslashes, and `blockedBy: DEV-21` native on the issue. The skill change is the third C10 branch, which had never had a fixture: `save_milestone` has no status field, but a Linear project milestone completes when its issues do, so moving the sole issue of `legC2-M3` to `done` produced one, and the next `list_milestones` still returned it at `progress: 100`. That answers the A23b question in Linear form and answers it the opposite way to GitHub, where `?state=all` is required or a closed milestone that still holds tickets reads as a name nothing matches. The file now says so, and says that a milestone inside an archived project remains unmeasured.

0.0.16 2026-09-19T22:25:28-07:00 Give `linear.md` a milestone read that the server accepts, and settle what `create` verification means on a backend that reformats markdown. Both come out of the first Linear leg, 11 PASS, 1 FAIL, 8 SKIP against `dev` in wkc-sandbox, which is also the first time C10 and C13 to C20 have ever been executed. F19, the skill defect: the per-verb table told the runner to pass `project` and `milestone` to `list_issues`, and that tool has no milestone filter at all, so the call is rejected with `Unrecognized key: "milestone"` and a milestone-scoped list is unreachable by the prescribed route. `projectMilestone` is a field you ask for in the result, not an argument you scope by, so the file now writes out the three steps that do work: resolve the name with `list_milestones`, page the project's issues for the requested status with `projectMilestone` in `fields`, filter locally. The unknown-name stop keeps its old shape and gains the reason an empty list is equally wrong, since an empty list reads as a real milestone nobody has filed against. `list_projects` with `includeMilestones: true` is named as the shortcut that is not one: it failed with `query is too complex, Complexity: 15879, Maximum allowed: 10000`, on workspace size rather than on anything the caller passed. F20, the contract conflict: C20 asked for a byte-for-byte description comparison on a backend whose own file already said the body does not round-trip, so the case was unsatisfiable as written and its FAIL measured the runbook. Linear returned 487 bytes for a 228-byte body, having inserted blank lines around headings and rewritten two bare issue ids into rich links, with every section, the code fence, and `café, 日本語, ✅` intact. C20 now checks that: sections present and in order, fence contents unchanged character for character, non-ASCII unchanged, and the native `blockedBy` relation on the issue, with a dropped section or an edited fence the FAIL. `SKILL.md`'s create row said to verify "the body and the status intended" across all three backends, which cannot mean bytes on Linear; it now says to compare the sections and their contents and that a backend reformatting markdown has not failed the write. `linear.md`'s round-trip bullet carries the measurement.

0.0.15 2026-09-19T14:23:33-07:00 Say what "alphanumeric" means in `local.md`'s slug rule, and move the slug check to the case that stresses it. The third local run closed out leg B's repaired cases, 13 of 13, and settled three things that were open: `create` does not invent an empty `## Blocked by` heading, which was the question behind F15 and is why B6's two hand-written fixtures are right rather than a workaround; B11 works in its third form, with two commits, the probe leaving the count unchanged and a scoped status read returning ` M`, and nothing hidden in `.git/info/exclude` to make it readable; and F13's missing-field clauses hold, since #098 carried `id`, `title` and `milestone` only and still read as `backlog` and unassigned, reaching the frontier after its move, which it could only do if the absent `assignee` read as nobody. F18, the skill change: the slug rule said to collapse every run of non-alphanumeric characters without saying what alphanumeric meant, and the two readings give different filenames for the same ticket, `émoji-and-日本語-and-ünïcödé` under Unicode against `-and--and-` under `[a-z0-9]`, which is the one thing writing the algorithm out is supposed to prevent. All thirteen skill-written filenames on that run match the Unicode reading, so the behaviour was already right and the rule merely permitted the other; it now says a Unicode letter or digit and carries that title as the worked example. F17, the runbook fix: F16 had put the slug check in B2 and illustrated it with B3's colon title, so the case could only be satisfied by another case's artifact, the same shape as F15. B2 now checks ticket A's own filename and the full ten-name check moves to B3. Also records that no local run has ever exercised the setup interview, because every run has supplied the answers in advance, and that leg D is where that belongs.

0.0.14 2026-09-19T08:37:33-07:00 Say what a partial frontmatter block means in `local.md`, and repair two runbook cases the first local leg exposed. The leg ran GREEN, 18 of 18, and the artifacts hold up under an independent read: all ten B3 titles round-trip as strings, so `0123` comes back `'0123'` and not `83` and `null` comes back `'null'` and not `None`; B18's body is byte-identical to its fixture with both `---` lines and the pasted `status: 'done'` intact and unread, which is the 0.0.13 frontmatter-boundary rule working. Two of the three fixes are to the runbook rather than the skill. B15 named ticket A on a ticket that has to be `ready`, but B9 moves A to `done` six cases earlier and the terminal rule correctly refuses to bring it back, so the case could not run as written; it now names the ticket that is `ready` and unassigned at that point and carries it through as `<R>` so the rest cannot drift back to A. That is F11, found by the run. B11 could not fail at all, which is F12, found verifying the run: it said to check `git status --porcelain` and expect the ticket files, but plain `--porcelain` collapses them into `?? docs/`, and the scratch repository had no commits at all, so an untracked tree was the default state whichever way the skill behaved. B11 now takes setup's step 7 commit offer first for a committed baseline and a clean tree, runs one mutating verb, and requires the commit count unchanged with that file listed as modified by `git status --porcelain -uall`. The skill change is F13: after a write verb touches a hand-written file, its frontmatter is real but partial, which is the ordinary state of such a ticket and a case `local.md` never covered, having described only a file with no frontmatter at all. The behaviour was already right, so this writes the rule down rather than changing it: a partial block is read field by field, a missing `id` comes from the filename exactly as it does when there is no frontmatter, a missing `status` is `backlog`, a missing `assignee` is unassigned, and silence about a field is never evidence it was cleared.

0.0.13 2026-09-19T07:45:35-07:00 Say where the frontmatter ends in `local.md`, and carry leg A's findings across to legs B and C, neither of which had been updated for anything leg A learned. `local.md` said only "read the frontmatter" and never said what bounds it, so a ticket body containing a `---`, as a horizontal rule or inside a pasted diff or config, is a parse error for a reader that splits on every one of them and, worse, a state change for a reader that takes the last block rather than the first: somebody's pasted `status: 'done'` becomes the ticket's status. The rule is now written down, the block between the first `---` and the next one, everything after it body that is never parsed, and B18 is the case for it, built like B3 out of the values that look harmless. Runbook, leg B: B17 reads a ticket with no `## Comments` section, which is F9's shape on a backend that has no flag to cause it; B7 gains A26's first-read rule, since files have no index to lag and a miss there is the skill's own; and B15's stranded sentence is put back in its paragraph. Leg C gains eight cases, having had none for anything since it was written: C13 to C15 cover the `assign` rework that changed `linear.md` and was tested nowhere, the single call that sets state and assignee together, the explicit forms and the reservation, and `move ... ready` writing `assignee: null`; C16 is A27's shape, an assignee Linear cannot resolve, which `linear.md` says nothing about; C17 is F7's, a write read back once with no retry, never measured on Linear where GitHub's index lagged by seconds; C18 is the one the tool schema gave away, since `list_issues` defaults `includeArchived` to false and nothing in `linear.md` mentions archiving, so a `list done` can come back short with nothing to say it did; C19 is F9's; C20 checks `create` wrote the description as sent, a markdown body crossing an MCP boundary being exactly where a silent rewrite would hide. C10 also gains the A23b question in Linear form, since `list_milestones` takes only a project and has no state or archived filter. C14's second half and C18 are marked [MANUAL], C14 for the second identity that C12 already needs and C18 because no MCP tool archives an issue.

0.0.12 2026-09-19T01:12:43-07:00 Replace the `claim` verb with `assign`, which can name somebody other than you, and give `move` a complete assignee rule. `claim` could only ever assign the caller, and only from `ready` with nobody on it, so a ticket somebody else half-finished could not be taken over and no ticket could be handed to a colleague at all. `assign <id>` bare is the old verb unchanged: it requires `ready` with no assignee and sets the assignee and `in-progress` in one write, because a ticket that is assigned while still `ready` sits outside the frontier and outside anybody's queue, so the gap between two writes is a hole to fall into. `assign <id> <who>`, `assign <id> <who> from <holder>` and `assign <id> none` set the assignee and never touch a label. Displacing an existing assignee requires naming them with `from`, matched case-insensitively against the read, because picking up free work and taking work out of somebody's hands are different acts and the second should have to be spelled out; an unnamed holder is a refusal rather than a silent removal, and the write removes every assignee found minus the target, the same subtract-the-target shape `move` already uses on labels for the same concurrent-mutation reason. The documentation also stops calling this race-safe, which it never was. There is no lock and no compare-and-set: both sessions can pass the precondition and both can write, and the tie-break is what makes them agree afterwards on which one won. It fires only where the backend keeps a list of assignees and the identities differ, never where two sessions share an account or where the backend has a single assignee field, and `SKILL.md` now says that in place of the stronger claim. `move` gains the assignee rule it was missing: `backlog` and **`ready`** both clear every assignee, `in-progress` and `in-review` leave them alone and report a holder who is not you, and a terminal move keeps the assignee as the record of who did the work. `ready` is the fix that matters and it was a live bug on all three backends: `next` filters on `ready` **and** no assignee, so a ticket handed back to `ready` with somebody still on it was invisible to the frontier and to that person at once, which is exactly the state a half-finished ticket landed in. New measurement behind the verification read, on `gh` 2.97.0: `gh issue edit --add-assignee` with a real user who has no push access prints the issue URL, exits 0, and assigns nobody, while a login that does not exist exits 1 with `Could not resolve to a user or bot with the login`. The silent one is the ordinary mistake, a teammate nobody added to the repository. Separately, `show` on GitHub becomes one call instead of two, because the first one could return nothing: `gh issue view <n> --comments` does not add the comments to the view, it replaces the issue with them, and on a ticket with none it prints nothing at all at exit 0, which reads exactly like a ticket that does not exist. `comments` is a valid `--json` field carrying author, timestamp and body, and it returns `[]` rather than silence, so it joins the field list `show` already reads and `--comments` is named in the file as the thing not to use. Found by the 0.0.11 validation re-run as F9. Runbook: A11, A20, B8 and C12 renamed onto the verb; A27 covers the silent no-op, A28 the unnamed holder, A29 and B16 the `move ... ready` clear, and B15 the explicit forms on local; Also settles what a `ready` ticket with an assignee means, since the two new rules would otherwise contradict each other: giving somebody a `ready` ticket reserves it, which drops it off the frontier on purpose so nobody else picks it up, while a name left on work that stopped is the thing `move <id> ready` clears. `assign` names a holder deliberately and moving into `ready` says nobody holds it, so the distinction is carried by which verb was used. A29b and B15 cover the reserved case. A24 is rewritten from one line into an executable two-account procedure, since the tie-break has never been observed and two accounts, not two terminals, is what proves it.

0.0.11 2026-09-19T00:49:51-07:00 Take the GitHub verbs off the search index, which is the real cause of the two findings the 0.0.10 validation run raised. `gh issue list` has two backends and the flags pick which one, shown with `GH_DEBUG=api` on `gh` 2.97.0: no filter, `--assignee` and `--state` send `query IssueList` over the repository's own issues, the primary store, while `--label`, `--milestone` and `--search` send a search query (`label:ready repo:<owner/repo> state:open type:issue`) to an index that runs behind. Measured three times in a row, an issue created with `--label ready` was in the primary-store read immediately on 3 of 3 trials and missing from `--label ready` on 3 of 3, still two tickets behind one trial later. That made `next` report an empty frontier exactly when a session had just moved tickets to `ready`, which is the ordinary case rather than a rare one, and no rule in either file distinguished it from a genuinely empty frontier. `next` now drops `--label ready` and `--search "no:assignee sort:created-asc"` and reads the same unfiltered page `list` already reads, doing the status test, the assignee test and the ordering in `jq` with `sort_by(.number)`; `list <status> <milestone>` drops `--milestone` and filters on `.milestone.title`, which the payload already carried. `github.md` gains a **Which reads are authoritative** section, and the truncation rule loses its advice to push ordering to the server with `--search "sort:created-asc"`, since that was what moved the read onto the index; raising `--limit` is the answer instead. `SKILL.md` states it for every backend: an empty frontier is never inferred from a read that is not the backend's authoritative one. Also retracts a claim 0.0.10 introduced. `github.md` said a closed milestone returns an empty list when queried by title while the number form returns its tickets, and that titles resolve against open milestones only. That does not reproduce: with the index settled, all four milestones on the scratch repository, two of them closed, returned identical results by title and by number, and `GH_DEBUG=api` shows `gh` translating `--milestone <number>` into `milestone:<title>` before sending it, so the two forms were always the same query. The 0.0.10 measurement was a closed milestone read by title seconds after the write and by number a minute later, which is the index lag above. What survives is the resolver and `?state=all`, now justified by the check they actually protect: `gh api repos/<R>/milestones` returns open milestones only, so without it a closed milestone holding tickets is reported as a name that does not exist. Adds that `gh issue create --milestone` and `gh issue edit --milestone` accept an open milestone only and exit 1 on a closed one, which is a real limit but a loud one. Runbook: A23b is rewritten to test the resolver against a closed milestone and to confirm the trap is armed before scoring, since its old discriminator was built on the retracted claim, and A26 is new, checking that a ticket moved to `ready` is in the very next `next` with no retry and no pause, where a retry is a FAIL rather than a PASS because scoring on a second read hides the defect. Findings F7 and F8 in the run log record what was measured and what changed.

0.0.10 2026-09-18T23:59:19-07:00 Fix the two FAIL cases from the GitHub leg of `validation/tracker.md`, both of which were skill defects rather than runbook errors. `tracker list <status> <milestone>` on GitHub could not stop on a milestone name that does not exist, because `github.md` said only to add `--milestone <title>` and `gh issue list --milestone no-such-milestone` exits 0 with an empty list and nothing on stderr, so a typo read back as "no tickets" while the same verb stopped on Linear. Measuring it produced a second, worse failure in the same place: `--milestone <title>` also returns `[]` at exit 0 for a **closed** milestone that has open tickets, while `--milestone <its number>` returns them, so a title lookup alone hides real work. `github.md` now resolves the title against `repos/<owner>/<repo>/milestones?state=all`, where `state=all` is required or a closed milestone reads as a name that does not exist, stops when nothing matches and names the milestones that do, and passes the resolved **number** to the query. All three behaviours measured against `gh` 2.97.0. The stop moves up into `SKILL.md` as a cross-backend guarantee, and `local.md` says what the milestone set is where there is no registry: the distinct non-empty `milestone` values, already in hand because `list` reads every file anyway. Second FAIL: `setup` asked Section A's GitHub prerequisites in the same picker as Sections B, C and D, so three sections were answered before anyone knew whether the chosen backend would work. The cause was a conflict inside `setup/SKILL.md` rather than a lost fix, between "one section, one answer" and "use the harness's own picker", where Claude Code's picker takes four questions per call and Codex has none, so the file read as consistent on the harness the runbook was written for. Step 2 now says a multi-question picker is a way to ask one section faster and never a way to put Sections A to D on screen together, and that only questions from the same section whose answers cannot change each other may share a call; step 1 says the same for the `git init` offer, which the run found batched with the interview. Three documentation defects from the same run fixed alongside: `create`'s verification in `github.md` asked for a body comparison and named no method, and both obvious methods report a difference on a byte-identical body, so it now gives `gh issue view <n> --json body | jq --rawfile sent <file> -e '.body == $sent'` and says why `--jq .body > file` and `"$(...)"` are wrong; the overlapping-label hazard is described as a race with no ordering guarantee, keeping both measurements, 8 of 8 in one session and 4 of 5 in another, rather than a count that reads as deterministic and that a single trial can contradict; and `stateReason: REOPENED` is named where the other three reasons are, with the reading rules now opening on "read `state` first", since a reopened issue is `OPEN` and anything switching on `stateReason` alone would read it as terminal. Runbook updated for the changed guarantees: A5 gives the exact body comparison instead of "the body as sent", A23 expects a stop naming the milestones that exist, A23b is new for the closed milestone, B14 is new for the local backend's milestone stop, and D5 is new for the picker, failing a run where `git init` and Sections A to D share one call even though every question in it is individually correct. Findings F1 to F6 in the run log carry what each fix was, with F1 recording that the fix is larger than the finding was.

0.0.9 2026-09-18T23:31:00-07:00 Revert the two Codex commits that tried to give `setup` a selectable picker, and keep the interview plain. They wrote a `request_user_input` call and a `codex --enable default_mode_request_user_input` launch flag into `SKILL.md` and `README.md`, and recorded in this file that the flag had been verified to open a direct picker on CLI 0.155.1. It does not, so the skill was instructing a model to call a mechanism that is not there, which is worse than the bullet list it replaced, and step 2 had grown from eight lines to forty carrying it. The conclusion kept instead: Codex has no picker a skill can invoke, so numbered options with the recommended one first and a digit accepted is the correct result there rather than a fallback to apologise for. No harness keystroke appears anywhere now: the one that prompted this is only Codex's rendering of a queued answer, and the rule is simply that a question ends its turn. `setup/SKILL.md` trimmed from 219 lines to 209 against AGENTS.md's 150-line guidance, tightening the Section A explainer, sections B and C, the config template paragraph, and the ownership rule without dropping a rule, and fixing a stale cross-reference that sent the reader to step 5 for the brownfield spec that step 6 writes.

0.0.8 2026-09-18T13:34:10-07:00 Add the `tracker` skill: one `SKILL.md` carrying the seven-status vocabulary (`backlog`, `ready`, `in-progress`, `in-review`, `done`, `cancel`, `duplicate`), eight verbs (`list`, `show`, `next`, `create`, `claim`, `comment`, `move`, `link`), and the ticket shape inline, plus one backend file per backend (`github.md`, `linear.md`, `local.md`) of which exactly one loads per session. On GitHub the four open states are labels and the three terminal states are native close reasons, so a closed issue carries no status label and an open issue with no label reads as `backlog`; `list backlog` therefore lists every open issue and keeps the unlabelled ones rather than filtering on the label. Dependencies are GitHub's native issue dependency edges, with no body-text fallback. Verbs that write now read first: `claim` requires exactly `ready` with no assignee before it edits, because `gh issue edit --remove-label` on an absent label exits 0 and would otherwise add `in-progress` beside `in-review`; `move` reads the current status and refuses to move out of a terminal state, because `gh issue close` on a closed issue exits 0 and silently keeps the original reason; `create` writes the ticket unlabelled, links its blockers, and applies the status last, so it never sits on the frontier before its edges exist. Verified against gh 2.97.0 that `blockedBy.totalCount` counts closed blockers as well as open ones, so the frontier query filters on `blockedBy.nodes[].state`, folds the 50-node cap into the jq, and lets a null `blockedBy` fail loudly rather than suppressing it into a permanently empty frontier. Lists are ordered server-side with `sort:created-asc` and treat a row count equal to `--limit` as incomplete, since `gh` returns newest first and would otherwise drop the lowest-numbered tickets `next` wants. Also verified that `stateReason: DUPLICATE` reads back from the `gh issue list` payload. `move <id> duplicate <original>` now carries the original's id. Linear resolves workflow states by type rather than by name, and stops rather than guessing when the MCP server exposes no relation tool. Local makes the frontmatter the only state anything reads, names `git config user.name` as the assignee, and compares ids numerically. `setup` now creates four status labels instead of seven, says the local backend is one session at a time, and points at `/tracker list` as the next step. Deletes the untracked half-ported `release` draft, which referenced a helper script and a workflow that do not exist here. Second review pass: the frontier jq now opens with an explicit `if .blockedBy == null then error(...)` rather than relying on the filters below it, because `null.totalCount` is `null` and `null.nodes | length` is `0` in jq, so a null row compared false against the 50-node check and was dropped silently at exit 0, which on a host without dependencies is a permanently empty frontier. Both filtering queries emit `rows`, the pre-filter count, since a full page that filters down to nothing is otherwise indistinguishable from an empty repository and the `--limit` rule had nothing to read. A session that loses the claim tie-break now removes only its own assignment and leaves the labels alone; stripping `in-progress` back to `ready` left the ticket assigned to the winner while reading as unclaimed. Linear reads `backlog` from every `triage` and `backlog` state, so a team whose new issues land in Triage does not hide the human-filed tickets `list backlog` exists to surface, and its `started` states resolve by workflow order rather than by name. Also states that the frontier is a candidate list rather than a set of facts, that `show` is the only verb reporting an issue with two status labels, that `create` takes an open status only and reports partial failure, and how `local` derives a slug, handles colliding ids, and writes frontmatter onto a hand-written file. Third review pass: `claim`'s GitHub pre-read requires exactly one status label rather than exactly the `ready` label, since a repository where every issue also carries `bug` would otherwise refuse every claim, and its re-read names the inconsistent label set as its own case. `move` removes every status label the read found instead of one guessed name, which makes it the repair for a ticket a human left carrying two. The terminal `list` gets its own jq so the `rows` truncation rule reaches it. The claim tie-break compares logins case-insensitively, because GitHub logins are. Linear gains the claim check it actually needs: its issues have a single assignee, so the multi-assignee race test cannot fire there and the re-read has to name you instead, with the remaining blind spot stated rather than papered over. A Linear state outside the seven is now reported by its own name and type instead of folding into `backlog`, which had made `show` call it `backlog` while `list backlog` never returned it, and the duplicate relation falls back to a comment when no relation tool exists. `local` no longer contradicts itself about the body: `link` keeps the `## Blocked by` section in step when the file has one and leaves a hand-written body alone when it does not. Fourth review pass: `move`'s removal list is every status label found **minus the target**, because `gh` sends label additions and removals as two concurrent GraphQL mutations (`pkg/cmd/pr/shared/editable_http.go`) with no ordering, so a label named in both lists ends in whichever lands last. Measured against gh 2.97.0, `--remove-label ready --add-label ready` on a `ready` ticket stripped the label in eight runs out of eight at exit 0, which made the most common move a skill issues, a no-op `move <id> ready`, silently reset the ticket to `backlog`. Keeping the two lists disjoint fixes the no-op, the `backlog`+`ready` repair, and the ordinary transition, all three verified live with a topic label attached. `claim`'s re-read now drops your assignment when the status came back wrong, instead of leaving your name on a ticket a human just moved. Fifth review pass: `claim`'s wrong-labels back-off removes the `in-progress` label it added rather than walking away from it, since that label is the session's own and leaving it behind handed the next reader a two-label ticket `claim` had created. The command has no add side, so it cannot hit the concurrent-mutation race, and removing an absent label is a no-op, so it is safe in every shape the re-read can return. Hardening pass: GitHub status resolution is one shared `jq` prelude (`STATUS`, `status_labels`, `status`) that every query prepends, so `list`, `next`, and `show` cannot disagree about one issue; an issue carrying two status labels resolves to `inconsistent`, is never returned as a ticket by any query, is always named in an `inconsistent` bucket beside the results, and is repaired by `move`. `list backlog` stops being a special case because the same expression serves all four open statuses. `SKILL.md` gains a read, write, verify section making the verification an independent read for all five mutating verbs, with a per-verb precondition and postcondition table, and `create`, `comment`, and `link` gain the verification reads they never had. The local backend single-quotes every frontmatter string value, doubling internal quotes, round-tripped through a YAML parser across 23 titles covering punctuation, leading zeros, bare `null` and `true`, dates, CJK, accented Latin, and emoji, since unquoted `0123` parses as `83` and unquoted `null` as a null. Linear stops rather than guessing when a state type does not identify exactly one write target, records the answer in a new `linear_states` config field that `setup` writes, and says plainly which parts of that file have been run against a real workspace (none) and which came from a real dogfood run (two findings). Docs give the Codex `$name` prefix wherever they gave a bare `/name`. Adds `validation/tracker.md`, a Codex-driven runbook of 43 numbered cases each with an independent check that decides PASS or FAIL, ending in a report, where a case that could not run is SKIP and never PASS and a case needing a second terminal, a second account, or a service with no credentials here is marked [MANUAL] and expected to be skipped. Runbooks are tracked and public on purpose, so a reviewer can read what a skill was tested against without installing anything and disagree with the coverage rather than only with the code; the cost is that they rot like any checked-in file, so the pre-commit checklist gains a fifth item requiring the runbook to be updated whenever a verb, a command, or a guarantee changes. AGENTS.md step 6 requires one per ported skill, and also records that documentation about a skill belongs in `skills/<name>/README.md`, which ships to every consumer because the installer copies the whole skill directory, while a runbook does not belong there because an adopter has no use for it. Adds `skills/tracker/README.md`, the human-facing explainer that ships with the skill: what the seven statuses mean, each of the eight verbs with its syntax, how the frontier decides what to work on next, why there is no `blocked` status, what each backend stores underneath, and why every write costs an extra read. `SKILL.md` stays the agent-facing instruction and the README does not repeat it. The ticket shape gains a `## Related` section for tickets that touch each other without one waiting on the other, stated as explicitly not the same kind of thing as `## Blocked by`: blockers become real dependency edges because `next` computes on them, while `## Related` is a reference nothing parses, writes, or queries. GitHub has no native relation for it, verified against the GraphQL schema, which exposes `blockedBy`, `blocking`, `parent`, `subIssues`, and `milestone` and no relation mutation at all, so a body reference is the honest representation and costs nothing because nothing reads it. `linear.md` gains the milestone mapping it was missing: a milestone is a project milestone inside the configured `linear_project` rather than a cycle, since a milestone names a body of work and a cycle is a time box, and a milestone name matching nothing is a stop rather than a silently unscoped list. Linear rewritten against a live workspace rather than the API docs, which changed the design and corrected several outright errors. There is no per-workspace status mapping and no `linear_states` config field: the seven statuses map onto Linear's seven default status names by exact name, and `state` on `save_issue` and `list_issues` accepts a name, verified. The status category is used once, by `setup`, purely to validate that the team's workflow covers the seven, since a name comparison cannot reveal an eighth status that tickets are being parked in; nothing filters or writes by category at run time. A missing required status is a stop with instructions for the team's settings, because no MCP tool creates or renames a status; an extra status is a loud warning plus a note written into the config body under a new **Tracker notes** section, so it stays a decision the user can return to. Corrections found by reading the real server: the tool names were guesses (`save_issue` creates and updates, there is no `create_issue` or `update_issue`, and comments are `save_comment`), relations are native parameters (`blockedBy`, `blocks`, `relatedTo`, `duplicateOf`) rather than something that may be absent, `duplicate` is its own category with its own status rather than a `canceled` fallback, statuses belong to the team and not the project, and `list_issue_statuses` returns them in no meaningful order. `next` is documented as costing one read per candidate plus one per blocker, because `list_issues` cannot return relations and a relation carries only the blocker's id and title, not its status. Linear write paths executed against a sandbox workspace, which corrected the file again. A Linear team created from the default template has six statuses and **no In Review**, so the missing-status stop is the ordinary first run rather than an edge case, and `setup` now says so instead of implying the user misconfigured something. The predecessor's "a status write with an inexact name fails silently" finding did **not** reproduce: an unknown name errors with `Could not find state "<name>"` and matching is case-insensitive, so that claim is retired and the runbook records what was actually seen. A new and worse finding replaces it: **marking an issue as a duplicate silently clears its other relations**, reproduced twice, with no error and no mention in the response, so `move duplicate` reads the relations first and reports what was lost. That move is also one call and not the obvious one: setting `duplicateOf` moves the status by itself, while creating an issue in a duplicate state is rejected and setting the status before the relation exists is rejected. `create` applies relations and status in one request on Linear, so it needs none of GitHub's create-link-then-label ordering. Claiming is one call with `state` and `assignee` together, and `assignee: null` clears it. Confirmed that a `blockedBy` relation carries only the blocker's id and title even after the blocker completes, so the frontier's per-blocker read is mandatory rather than merely convenient. Runbook and pull request reconciled against the skill: `validation/tracker.md` goes to 53 numbered cases from 43, renumbered sequentially rather than carrying letter suffixes, and gains the cases for the verbs and guarantees that landed after it was written. New on GitHub: `link` as its own verb, since the dependency endpoint takes the blocker's numeric database id rather than its issue number and a skill that passes the number either errors or writes an edge to an unrelated issue; `## Related` producing a body reference and no dependency edge, since a reference that became an edge would drop the ticket off the frontier; and a milestone read as the second `list` argument, with a name matching nothing a stop rather than a silently unscoped list. New on local: `## Related` gaining no frontmatter key, and `link` keeping the `## Blocked by` body section in step where the file has one while leaving a hand-written body alone. New on Linear: the comment path, which is the one read-write path in that file never executed, and the claim race, which needs two identities. The Linear leg no longer says every write path there is unexecuted, because the writes were run against a sandbox. `linear.md` loses the claim that Linear's default status names mean most teams already match, which its own next paragraph contradicted: six of the seven names are defaults and `In Review` is not, which is exactly why the missing-status stop is the ordinary first run. Fix two `setup` defects found running the runbook's GitHub leg by hand on Codex. `setup` checked the GitHub prerequisites at step 5, three sections after the Section A answer that depends on them, so choosing `github` in a directory with no remote produced a note and then three more interview sections before anything was done about it; the checks move into Section A, where a missing remote becomes a question offering both `git remote add origin <url>` and `gh repo create`, and a disconnected Linear MCP server becomes a stop. Step 5 keeps only the one-time work the prerequisites enable. Step 1 also never checked whether the directory was a git repository at all, which is the documented greenfield case and which every backend needs by step 7, so it now reads `git rev-parse --git-dir` and offers `git init` before the interview starts. Step 2 gains the rule that a choice is offered rather than typed: a native option picker where the harness has one, a numbered list where it does not, and never a path the user has to retype. The runbook goes to 56 cases: two new at the head of the GitHub leg for the missing repository and the missing remote, which renumbers that leg, and one in harness parity for how the choices are rendered. Drop the `mkdir -p .claude .kiro` workaround from the install instructions and from the runbook's setup block. vercel-labs/skills#2071 is fixed and closed, by PR #2216, which landed one second before the issue closed and shipped an hour later. Boundary tested rather than inferred from the dates: against skills 1.5.25 a project install with `-a kiro-cli` creates no `.kiro/` at all and reports success, and against 1.5.26 it creates the directory and the symlink. The README now names 1.5.26 as the fix and keeps `mkdir -p .kiro` only as the workaround for anyone pinned below it, and runbook case S2 gains the check that both agent directories were created by the installer, so the regression has somewhere to show up. Drop `git init` from the runbook's setup block as well. Verified that the installer works in a directory that is not a git repository at all, so the line was never needed, and it pre-empted the case directly below it: A1 exists to check that `setup` notices a missing repository and offers to create one, which it can never observe if the runbook created it first. Same defect shape as the `mkdir` workaround, which had been masking S2. The GitHub leg now names its three starting states and runs the rest of itself against the third: no repository at all, a repository with no remote, and a clone of one that already exists, which is the ordinary case and previously went untested. A run against a repository created through GitHub's web UI also corrected the label case: `duplicate` is in GitHub's own default label set, so expecting it absent after setup would fail on any such repository. The check now covers only `done` and `cancel`, and `duplicate` becomes the sharp half of the topic-label case instead, because it shares a name with one of the seven statuses while not being a status label at all, so it has to survive a claim and a terminal close untouched. Teardown no longer tells the runner to delete a repository they already owned. A question now ends the turn. Running Section A on Codex, `setup` asked which tracker to use and then kept working for over a minute, which left the harness unable to take a plain reply and parked the answer as a queued input. Asking and then continuing is the defect, so step 2 states that a question is the last thing in its turn, with no tool call after it and no work started while it is outstanding. The same rule goes into AGENTS.md rather than only into `setup`, because every skill left on the roster interviews somebody. Runbook case D5 watches the harness while the question is on screen and fails the run if it reports work in progress or a queued input.
0.0.7 2026-08-28T22:15:00-07:00 Turn `delete_branch_on_merge` back off and write down why: a merge is sometimes what triggers the check being waited on, so the branch has to outlive the merge and gets deleted once the work is verified. AGENTS.md says a merged branch is not a dead branch, and CONTRIBUTING.md's Rulesets section becomes "Rulesets and settings", recording the two plain repository settings (auto-delete off, wiki off) alongside the ruleset payloads.
0.0.6 2026-08-28T17:05:00-07:00 Apply the two repository rulesets borrowed from agent-toolkit, both active with an empty bypass_actors so they bind the owner: `main pull request gate` on the default branch (deletion, non_fast_forward, pull_request at zero required approvals) and `Immutable release tags` on refs/tags/v* (deletion, non_fast_forward, update, no creation rule). Verified by attempting each violation: a direct push to main is rejected with GH013, moving v0.0.5 gets "Cannot update this protected ref", deleting it gets "Cannot delete this tag". Skips agent-toolkit's required_status_checks rule, which points at a repository-validation workflow this repo deliberately does not have. AGENTS.md and CONTRIBUTING.md now describe the gate as enforced and record both payloads.
0.0.5 2026-08-28T13:20:00-07:00 Put `description` on one unquoted physical line, in skills/setup/SKILL.md and in the template. Kiro CLI's slash-command menu takes whatever follows `description:` on that one line rather than parsing the YAML, so a folded block rendered as `/setup    >` and a quoted string would keep its quotes; the model-facing copy of the same frontmatter is parsed correctly, which is why this only shows up in the menu. Reproduced and fixed against a real kiro-cli session. States the rule in AGENTS.md and in skill-template/README.md.
0.0.4 2026-08-28T00:45:00-07:00 Rename the skeleton to skill-template/SKILL.md.template. The installer discovers skills by finding any SKILL.md in the repository, not by reading skills/, so v0.0.3 shipped the template to consumers as an installable skill named skill-name. Confirmed fixed with `npx skills add 'https://github.com/wilsonkichoi/skills.git#fix/template-not-a-skill' -l`, which reports one skill. Corrects the pinned install syntax everywhere: pinning needs the full git URL with a #ref, where a missing ref fails loudly, while the owner/repo@vX.Y.Z shorthand treats @ as a skill selector and silently installs from the default branch when paired with -s.
0.0.3 2026-08-26T13:19:38-07:00 Add Kiro CLI as a supported harness: install path, /name invocation, and the fact that Kiro has no disable-model-invocation equivalent, so a Kiro user can trigger a skill by conversation alone, which is accepted; SKILL.md frontmatter now also carries metadata: allow_implicit_invocation: "false" as a quoted string, on the chance a harness starts reading it. Updates AGENTS.md, README, and the setup skill doc block. Install instructions now name the agents with `-a` and pre-create `.kiro/`, which vercel-labs/skills#2071 requires. Adds an Uninstall section, including the vercel-labs/skills#1771 warning that a sweeping `remove` deletes this repository's own `skills/` source through the OpenClaw path. Adds a skill-template/ folder holding the copyable SKILL.md and agents/openai.yaml skeletons plus a README that writes out the Agent Skills specification rules in full, so no session has to fetch the spec; AGENTS.md points at the folder instead of carrying the template inline, and the prose rule now asks for sentences you would say out loud instead of banning specific words.
0.0.2 2026-08-25T22:48:06-07:00 Fill in the root docs (README, CONTRIBUTING, CHANGELOG), correct AGENTS.md to match the repository as it is, and finish the setup skill: consistent docs/dev-agents/ paths, context_file and Linear questions in the interview, per-backend tracker setup, and a config template whose fields match what the interview collects.
0.0.1 2026-08-17T10:59:57-07:00 Initial scaffolding: LICENSE, AGENTS.md, CLAUDE.md, VERSION, and the first draft of the setup skill.
